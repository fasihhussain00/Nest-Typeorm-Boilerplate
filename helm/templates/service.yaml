apiVersion: v1
kind: Service
metadata:
  name: {{ include "ts-node-boilerplate.fullname" . }}
  labels:
    {{- include "ts-node-boilerplate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ts-node-boilerplate.selectorLabels" . | nindent 4 }}
