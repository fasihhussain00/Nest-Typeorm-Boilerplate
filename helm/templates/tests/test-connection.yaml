apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ts-node-boilerplate.fullname" . }}-test-connection"
  labels:
    {{- include "ts-node-boilerplate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ts-node-boilerplate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
